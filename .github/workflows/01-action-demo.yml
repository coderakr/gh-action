name: 01-ak flask CI workflow

on:
  push:

jobs:
    flask-ci-job:
        runs-on: ubuntu-latest

        steps:
          - name: checkout Repository
            uses: actions/checkout@v6
          
          - name: Build docker image
            run: docker build -t ak-flask-app:v1.0.0 .

          - name: Run container
            run: docker run -d -p 5000:5000 --name ak-flask-container ak-flask-app:v1.0.0

          - name: Smoke Test
            run: |
              sleep 5
              curl http://localhost:5000/health
