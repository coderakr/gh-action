name: print var name
on:
    push:

jobs:
    test-var:
        runs-on: ubuntu-latest

        steps:  
            - name: Variables name
              run: |
                echo "repo: $GITHUB_ACTION_REPOSITORY"
                echo "Actor name: $GITHUB_ACTOR"
                echo "Actor Id: $GITHUB_ACTOR_ID"
                echo "env: $GITHUB_ENV"
                cat /home/runner/work/_temp/_runner_file_commands/set_env_7d7b72e9-d034-46be-ad30-2d10457a9d29